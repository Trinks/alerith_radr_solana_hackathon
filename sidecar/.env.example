# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet

# Wallet Secret Keys (base58 encoded)
# CRITICAL: Never commit real keys! Use environment variables or secrets manager.
ESCROW_WALLET_SECRET=
TREASURY_WALLET_SECRET=
SERVER_AUTHORITY_SECRET=

# Security
# CRITICAL: Generate a secure 32+ byte random hex string
# Example: openssl rand -hex 32
WALLET_PEPPER=

# Internal API authentication (game server → sidecar)
# CRITICAL: Generate a secure random string
# Example: openssl rand -hex 32
INTERNAL_API_KEY=

# Stake Configuration (minimums are per-token, defined in code)
HOUSE_FEE_PERCENT=2

# Server
PORT=3002
NODE_ENV=development
